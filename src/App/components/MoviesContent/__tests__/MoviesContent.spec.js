import React from 'react';
import { mount } from 'enzyme';

import MoviesContent from '../MoviesContent';


describe('Movies content behavior test', () => {
    it('movies should load after render', () => {
    });
});
